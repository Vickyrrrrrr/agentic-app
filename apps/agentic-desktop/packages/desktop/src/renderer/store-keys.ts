/**
 * Store name constants for the Electron renderer process.
 * These must match the store names used in the main process (store-keys.ts)
 * and in the app package (persist.ts).
 */
export const GLOBAL_STORE = "opencode.global.dat"
export const SETTINGS_STORE = "opencode.settings"
export const SETTINGS_DAT_STORE = "opencode.settings.dat"
